import { defineConfig } from 'astro/config';

export default defineConfig({
  // Once your custom domain is connected, put it here (e.g. 'https://nextgenwealthbeat.com')
  // so link previews and SEO tags use the right address.
  site: 'https://example.com',
});
